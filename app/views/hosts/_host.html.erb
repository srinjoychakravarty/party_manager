<li>
	<%= link_to host.first_name, host %>
	<% if current_host.admin? %>
		<%= link_to "delete", host, :method => delete, :confirm => "Are you sure?", :title => "Delete #{host.first_name}" %>
	<% end %>
</li>